import { bigint, boolean, date, integer, jsonb, numeric, pgTable, text, timestamp, uuid, varchar, } from "drizzle-orm/pg-core";
export const warehousesTable = pgTable("warehouses", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    branchId: uuid("branch_id"),
    warehouseCode: varchar("warehouse_code").unique(),
    warehouseName: varchar("warehouse_name"),
    warehouseType: varchar("warehouse_type"),
    addressId: uuid("address_id"),
    contactName: varchar("contact_name"),
    contactEmail: varchar("contact_email"),
    contactPhone: varchar("contact_phone"),
    timezone: varchar("timezone"),
    isDefault: boolean("is_default"),
    isActive: boolean("is_active"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
    deletedAt: timestamp("deleted_at"),
    version: integer("version"),
    description: text("description"),
    status: varchar("status").default("draft"),
    country: varchar("country"),
    stateProvince: varchar("state_province"),
    city: varchar("city"),
    postalCode: varchar("postal_code"),
    addressLine1: varchar("address_line1"),
    addressLine2: varchar("address_line2"),
    latitude: numeric("latitude"),
    longitude: numeric("longitude"),
    totalCapacity: integer("total_capacity"),
    capacityUnit: varchar("capacity_unit").default("Units/Pallets"),
    numberZones: integer("number_zones"),
    numberRacks: integer("number_racks"),
    numberLevels: integer("number_levels"),
    temperatureControlled: boolean("temperature_controlled").default(false),
    minTemperature: numeric("min_temperature"),
    maxTemperature: numeric("max_temperature"),
    hazardousMaterialAllowed: boolean("hazardous_material_allowed").default(false),
    alternateContactName: varchar("alternate_contact_name"),
    alternateContactEmail: varchar("alternate_contact_email"),
    alternateContactPhone: varchar("alternate_contact_phone"),
    emergencyContactName: varchar("emergency_contact_name"),
    emergencyContactPhone: varchar("emergency_contact_phone"),
    operatingHours: jsonb("operating_hours"),
    allowCrossDocking: boolean("allow_cross_docking").default(false),
    allowBulkStorage: boolean("allow_bulk_storage").default(false),
    allowHazardousStorage: boolean("allow_hazardous_storage").default(false),
    requiresAdvanceNotice: boolean("requires_advance_notice").default(false),
    advanceNoticeHours: integer("advance_notice_hours"),
    defaultReceivingZoneId: uuid("default_receiving_zone_id"),
    defaultShippingZoneId: uuid("default_shipping_zone_id"),
    integrationType: varchar("integration_type"),
});
export const warehouseZonesTable = pgTable("warehouse_zones", {
    id: uuid("id").primaryKey(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    zoneCode: varchar("zone_code"),
    zoneName: varchar("zone_name"),
    qrCode: varchar("qr_code"),
    qrIdentifier: varchar("qr_identifier"),
    qrPayload: text("qr_payload"),
    qrChecksum: varchar("qr_checksum"),
    zoneType: varchar("zone_type"),
    temperatureType: varchar("temperature_type"),
    allowsPick: boolean("allows_pick"),
    allowsPutaway: boolean("allows_putaway"),
    allowsShipping: boolean("allows_shipping"),
    allowsReceiving: boolean("allows_receiving"),
    sequenceOrder: integer("sequence_order"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
    description: text("description"),
    status: varchar("status").default("draft"),
    primaryPurpose: varchar("primary_purpose"),
    allowCrossDocking: boolean("allow_cross_docking").default(false),
    allowBulkStorage: boolean("allow_bulk_storage").default(false),
    allowHazardousStorage: boolean("allow_hazardous_storage").default(false),
    allowReturnsProcessing: boolean("allow_returns_processing").default(false),
    allowQualityCheck: boolean("allow_quality_check").default(false),
    totalCapacity: integer("total_capacity").default(0),
    capacityUnit: varchar("capacity_unit").default("Pallets"),
    numberRacks: integer("number_racks").default(0),
    numberLevels: integer("number_levels").default(0),
    numberAisles: integer("number_aisles").default(0),
    width: numeric("width"),
    length: numeric("length"),
    height: numeric("height"),
    temperatureControlled: boolean("temperature_controlled").default(false),
    temperatureRange: varchar("temperature_range"),
    humidityControlled: boolean("humidity_controlled").default(false),
    humidityRange: varchar("humidity_range"),
    lightingType: varchar("lighting_type"),
    ventilationRequired: boolean("ventilation_required").default(false),
    restrictedAccess: boolean("restricted_access").default(false),
    requiredAccessLevel: varchar("required_access_level"),
    securityCameraCoverage: boolean("security_camera_coverage").default(false),
    visitorAllowed: boolean("visitor_allowed").default(false),
    biometricRequired: boolean("biometric_required").default(false),
    requiresPpe: boolean("requires_ppe").default(false),
    accessNotes: text("access_notes"),
    constraintTempSensitive: boolean("constraint_temp_sensitive").default(false),
    constraintHumiditySensitive: boolean("constraint_humidity_sensitive").default(false),
    constraintHazardousMaterials: boolean("constraint_hazardous_materials").default(false),
    constraintHighValueArea: boolean("constraint_high_value_area").default(false),
    constraintSecurityRestricted: boolean("constraint_security_restricted").default(false),
    constraintHeavyLoadOnly: boolean("constraint_heavy_load_only").default(false),
    constraintFragileItemsOnly: boolean("constraint_fragile_items_only").default(false),
    constraintFefoExpiry: boolean("constraint_fefo_expiry").default(false),
});
export const aislesTable = pgTable("aisles", {
    id: uuid("id").primaryKey(),
    warehouseZoneId: uuid("warehouse_zone_id").references(() => warehouseZonesTable.id),
    aisleCode: varchar("aisle_code"),
    aisleName: varchar("aisle_name"),
    qrCode: varchar("qr_code"),
    qrIdentifier: varchar("qr_identifier"),
    qrPayload: text("qr_payload"),
    qrChecksum: varchar("qr_checksum"),
    sequenceOrder: integer("sequence_order"),
});
export const racksTable = pgTable("racks", {
    id: uuid("id").primaryKey(),
    aisleId: uuid("aisle_id").references(() => aislesTable.id),
    rackCode: varchar("rack_code"),
    rackName: varchar("rack_name"),
    qrCode: varchar("qr_code"),
    qrIdentifier: varchar("qr_identifier"),
    qrPayload: text("qr_payload"),
    qrChecksum: varchar("qr_checksum"),
    maxWeightCapacity: numeric("max_weight_capacity"),
    maxVolumeCapacity: numeric("max_volume_capacity"),
});
export const shelvesTable = pgTable("shelves", {
    id: uuid("id").primaryKey(),
    rackId: uuid("rack_id").references(() => racksTable.id),
    shelfCode: varchar("shelf_code"),
    shelfName: varchar("shelf_name"),
    qrCode: varchar("qr_code"),
    qrIdentifier: varchar("qr_identifier"),
    qrPayload: text("qr_payload"),
    qrChecksum: varchar("qr_checksum"),
    maxWeightCapacity: numeric("max_weight_capacity"),
    maxVolumeCapacity: numeric("max_volume_capacity"),
});
export const binLocationsTable = pgTable("bin_locations", {
    id: uuid("id").primaryKey(),
    shelfId: uuid("shelf_id").references(() => shelvesTable.id),
    warehouseZoneId: uuid("warehouse_zone_id").references(() => warehouseZonesTable.id),
    binCode: varchar("bin_code").unique(),
    barcode: varchar("barcode"),
    qrCode: varchar("qr_code"),
    qrIdentifier: varchar("qr_identifier"),
    qrPayload: text("qr_payload"),
    qrChecksum: varchar("qr_checksum"),
    binType: varchar("bin_type"),
    maxQuantity: numeric("max_quantity"),
    maxWeight: numeric("max_weight"),
    maxVolume: numeric("max_volume"),
    currentWeight: numeric("current_weight"),
    currentVolume: numeric("current_volume"),
    isPickable: boolean("is_pickable"),
    isReceivingBin: boolean("is_receiving_bin"),
    isShippingBin: boolean("is_shipping_bin"),
    isQuarantineBin: boolean("is_quarantine_bin"),
    isActive: boolean("is_active"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
    companyId: uuid("company_id"),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    rack: varchar("rack"),
    level: varchar("level"),
    position: varchar("position"),
    generatedBinCode: varchar("generated_bin_code"),
    binName: varchar("bin_name"),
    description: text("description"),
    status: varchar("status").default("Active"),
    sizePreset: varchar("size_preset"),
    length: numeric("length"),
    width: numeric("width"),
    height: numeric("height"),
    volume: numeric("volume"),
    weightCapacity: numeric("weight_capacity"),
    unitCapacity: numeric("unit_capacity"),
    storageType: varchar("storage_type"),
    maxSkus: integer("max_skus"),
    allowOversize: boolean("allow_oversize").default(false),
    allowHeavy: boolean("allow_heavy").default(false),
    allowFragile: boolean("allow_fragile").default(false),
    allowHazardous: boolean("allow_hazardous").default(false),
    requiresLiftingEquipment: boolean("requires_lifting_equipment").default(false),
    pickingPriority: varchar("picking_priority"),
    pickingSequence: integer("picking_sequence"),
    fastMover: boolean("fast_mover").default(false),
    replenishmentSource: varchar("replenishment_source"),
    replenishmentThreshold: numeric("replenishment_threshold"),
    maxPickingQuantity: numeric("max_picking_quantity"),
    constraintTempSensitive: boolean("constraint_temp_sensitive").default(false),
    constraintHumiditySensitive: boolean("constraint_humidity_sensitive").default(false),
    constraintHighValueSecurity: boolean("constraint_high_value_security").default(false),
    constraintFefo: boolean("constraint_fefo").default(false),
    constraintFifo: boolean("constraint_fifo").default(false),
    constraintLifo: boolean("constraint_lifo").default(false),
    constraintCycleCountRequired: boolean("constraint_cycle_count_required").default(false),
    constraintQualityCheckRequired: boolean("constraint_quality_check_required").default(false),
    pickingMethod: varchar("picking_method"),
});
export const putawayRuleGroupsTable = pgTable("putaway_rule_groups", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    groupName: varchar("group_name").notNull(),
    description: text("description"),
    priority: integer("priority").default(10).notNull(),
    isActive: boolean("is_active").default(true),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
});
export const putawayRulesTable = pgTable("putaway_rules", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    ruleName: varchar("rule_name").notNull(),
    ruleCode: varchar("rule_code").notNull(),
    ruleType: varchar("rule_type").notNull(),
    status: varchar("status").default("Active").notNull(),
    description: text("description"),
    matchType: varchar("match_type").default("AND").notNull(),
    // Conditions
    categoryId: uuid("category_id").references(() => productCategoriesTable.id),
    abcClass: varchar("abc_class"),
    movementSpeed: varchar("movement_speed"),
    hasExpiry: boolean("has_expiry").default(false),
    requiresColdChain: boolean("requires_cold_chain").default(false),
    weightMin: numeric("weight_min"),
    weightMax: numeric("weight_max"),
    sku: varchar("sku"),
    inventoryCharacteristics: varchar("inventory_characteristics"),
    // Target Location
    targetZoneId: uuid("target_zone_id").references(() => warehouseZonesTable.id),
    targetBinType: varchar("target_bin_type"),
    targetBinId: uuid("target_bin_id").references(() => binLocationsTable.id),
    fallbackZoneId: uuid("fallback_zone_id").references(() => warehouseZonesTable.id),
    // Execution Settings
    priority: integer("priority").default(10).notNull(),
    stopOnMatch: boolean("stop_on_match").default(true),
    considerAvailableCapacity: boolean("consider_available_capacity").default(true),
    applyDuring: jsonb("apply_during"),
    validFrom: timestamp("valid_from"),
    validTo: timestamp("valid_to"),
    // Group Relation
    ruleGroupId: uuid("rule_group_id").references(() => putawayRuleGroupsTable.id),
    // Metadata
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
    createdBy: varchar("created_by"),
    modifiedBy: varchar("modified_by"),
});
export const slottingStrategiesTable = pgTable("slotting_strategies", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    strategyName: varchar("strategy_name").notNull(),
    strategyCode: varchar("strategy_code").notNull(),
    description: text("description"),
    priority: integer("priority").default(10).notNull(),
    isActive: boolean("is_active").default(true),
    criteria: jsonb("criteria"),
    targetZoneId: uuid("target_zone_id").references(() => warehouseZonesTable.id),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
});
export const slottingProfilesTable = pgTable("slotting_profiles", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    profileName: varchar("profile_name", { length: 120 }).notNull(),
    optimizationBasis: varchar("optimization_basis", { length: 60 }).notNull(),
    secondaryFactor: varchar("secondary_factor", { length: 80 }).notNull(),
    tertiaryFactor: varchar("tertiary_factor", { length: 80 }).notNull(),
    constraints: jsonb("constraints").notNull(),
    weights: jsonb("weights").notNull(),
    isDefault: boolean("is_default").default(false).notNull(),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
});
export const slottingParametersTable = pgTable("slotting_parameters", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    profileId: uuid("profile_id"),
    parameters: jsonb("parameters").notNull(),
    calculationDate: timestamp("calculation_date").defaultNow().notNull(),
    updatedBy: uuid("updated_by"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
});
export const slottingRunsTable = pgTable("slotting_runs", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    profileId: uuid("profile_id"),
    runNumber: varchar("run_number", { length: 80 }).notNull(),
    status: varchar("status", { length: 40 }).default("completed").notNull(),
    optimizationScore: numeric("optimization_score").default("0").notNull(),
    potentialImprovement: numeric("potential_improvement").default("0").notNull(),
    recommendedMoves: integer("recommended_moves").default(0).notNull(),
    distanceReduction: numeric("distance_reduction").default("0").notNull(),
    laborTimeSavings: numeric("labor_time_savings").default("0").notNull(),
    summary: jsonb("summary").notNull(),
    parameters: jsonb("parameters").notNull(),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const slottingRecommendationsTable = pgTable("slotting_recommendations", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    runId: uuid("run_id").notNull(),
    priority: varchar("priority", { length: 20 }).notNull(),
    sku: varchar("sku", { length: 120 }).notNull(),
    productName: varchar("product_name").notNull(),
    productImage: text("product_image"),
    productCode: varchar("product_code", { length: 120 }),
    currentLocation: varchar("current_location", { length: 120 }).notNull(),
    currentZone: varchar("current_zone", { length: 120 }),
    currentBinId: uuid("current_bin_id"),
    recommendedLocation: varchar("recommended_location", { length: 120 }).notNull(),
    recommendedZone: varchar("recommended_zone", { length: 120 }),
    recommendedBinId: uuid("recommended_bin_id"),
    moveType: varchar("move_type", { length: 40 }).notNull(),
    distanceSaved: numeric("distance_saved").default("0").notNull(),
    estimatedTimeSaved: numeric("estimated_time_saved").default("0").notNull(),
    optimizationScore: numeric("optimization_score").default("0").notNull(),
    reason: text("reason").notNull(),
    recommendationConfidence: numeric("recommendation_confidence").default("0").notNull(),
    velocityClass: varchar("velocity_class", { length: 20 }).notNull(),
    status: varchar("status", { length: 30 }).default("pending").notNull(),
    approvedBy: uuid("approved_by"),
    rejectedBy: uuid("rejected_by"),
    decisionNote: text("decision_note"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
});
export const slottingTasksTable = pgTable("slotting_tasks", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    recommendationId: uuid("recommendation_id"),
    taskCode: varchar("task_code", { length: 80 }).notNull(),
    status: varchar("status", { length: 30 }).default("pending").notNull(),
    priority: varchar("priority", { length: 20 }).notNull(),
    sku: varchar("sku", { length: 120 }).notNull(),
    productName: varchar("product_name").notNull(),
    currentLocation: varchar("current_location", { length: 120 }).notNull(),
    recommendedLocation: varchar("recommended_location", { length: 120 }).notNull(),
    assignedOperatorId: uuid("assigned_operator_id"),
    assignedZoneId: uuid("assigned_zone_id"),
    estimatedCompletionMinutes: integer("estimated_completion_minutes"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
});
export const slottingTaskAssignmentsTable = pgTable("slotting_task_assignments", {
    id: uuid("id").primaryKey(),
    taskId: uuid("task_id").notNull(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    operatorId: uuid("operator_id"),
    zoneId: uuid("zone_id"),
    assignedBy: uuid("assigned_by"),
    assignedAt: timestamp("assigned_at").defaultNow().notNull(),
});
export const slottingMoveHistoryTable = pgTable("slotting_move_history", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    recommendationId: uuid("recommendation_id"),
    taskId: uuid("task_id"),
    sku: varchar("sku", { length: 120 }),
    fromLocation: varchar("from_location", { length: 120 }),
    toLocation: varchar("to_location", { length: 120 }),
    moveType: varchar("move_type", { length: 40 }),
    status: varchar("status", { length: 30 }).notNull(),
    performedBy: uuid("performed_by"),
    createdAt: timestamp("created_at").defaultNow().notNull(),
});
export const slottingVelocityAnalysisTable = pgTable("slotting_velocity_analysis", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    runId: uuid("run_id").notNull(),
    sku: varchar("sku", { length: 120 }).notNull(),
    productId: uuid("product_id"),
    currentBinId: uuid("current_bin_id"),
    velocityClass: varchar("velocity_class", { length: 20 }).notNull(),
    pickFrequency: integer("pick_frequency").default(0).notNull(),
    units30Days: numeric("units_30_days").default("0").notNull(),
    abcClass: varchar("abc_class", { length: 10 }).notNull(),
    replenishmentFrequency: integer("replenishment_frequency").default(0).notNull(),
    slotUtilization: numeric("slot_utilization").default("0").notNull(),
    createdAt: timestamp("created_at").defaultNow(),
});
export const slottingLayoutSnapshotsTable = pgTable("slotting_layout_snapshots", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    runId: uuid("run_id").notNull(),
    snapshotType: varchar("snapshot_type", { length: 30 }).notNull(),
    layout: jsonb("layout").notNull(),
    createdAt: timestamp("created_at").defaultNow(),
});
export const slottingAnalyticsTable = pgTable("slotting_analytics", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    runId: uuid("run_id").notNull(),
    metricKey: varchar("metric_key", { length: 80 }).notNull(),
    metricValue: jsonb("metric_value").notNull(),
    createdAt: timestamp("created_at").defaultNow(),
});
export const slottingActivityLogsTable = pgTable("slotting_activity_logs", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    runId: uuid("run_id"),
    eventType: varchar("event_type", { length: 80 }).notNull(),
    oldValue: jsonb("old_value"),
    newValue: jsonb("new_value"),
    performedBy: uuid("performed_by"),
    createdAt: timestamp("created_at").defaultNow().notNull(),
});
export const slottingOptimizationsTable = pgTable("slotting_optimizations", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    runId: uuid("run_id"),
    scenarioName: varchar("scenario_name", { length: 120 }),
    description: text("description"),
    parameters: jsonb("parameters").notNull(),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const tasksTable = pgTable("tasks", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    organizationId: uuid("organization_id"),
    zoneId: uuid("zone_id"),
    taskNumber: varchar("task_number", { length: 80 }).notNull(),
    title: varchar("title", { length: 180 }).notNull(),
    taskType: varchar("task_type", { length: 80 }).notNull(),
    priority: varchar("priority", { length: 20 }).default("medium").notNull(),
    description: text("description"),
    relatedOrderId: uuid("related_order_id"),
    relatedOrderNumber: varchar("related_order_number", { length: 100 }),
    relatedWaveId: uuid("related_wave_id"),
    relatedWaveNumber: varchar("related_wave_number", { length: 100 }),
    locationLabel: varchar("location_label", { length: 160 }),
    pickMethod: varchar("pick_method", { length: 80 }),
    pickStrategy: varchar("pick_strategy", { length: 80 }),
    pickStation: varchar("pick_station", { length: 80 }),
    assigneeId: uuid("assignee_id"),
    assigneeName: varchar("assignee_name", { length: 160 }),
    dueDate: date("due_date"),
    dueTime: varchar("due_time", { length: 20 }),
    estimatedDurationMinutes: integer("estimated_duration_minutes").default(0),
    workShift: varchar("work_shift", { length: 120 }),
    status: varchar("status", { length: 40 }).default("to_do").notNull(),
    progressPercent: integer("progress_percent").default(0).notNull(),
    completedUnits: integer("completed_units").default(0).notNull(),
    totalUnits: integer("total_units").default(0).notNull(),
    totalItems: integer("total_items").default(0).notNull(),
    tags: jsonb("tags").default([]).notNull(),
    requiresApproval: boolean("requires_approval").default(false).notNull(),
    approverId: uuid("approver_id"),
    batchTask: boolean("batch_task").default(false).notNull(),
    batchSize: integer("batch_size"),
    specialHandling: varchar("special_handling", { length: 120 }),
    specialInstructions: text("special_instructions"),
    internalNotes: text("internal_notes"),
    taskPreview: jsonb("task_preview").default({}).notNull(),
    metadata: jsonb("metadata").default({}).notNull(),
    createdBy: uuid("created_by"),
    updatedBy: uuid("updated_by"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
    deletedAt: timestamp("deleted_at"),
});
export const taskAssignmentsTable = pgTable("task_assignments", {
    id: uuid("id").primaryKey(),
    taskId: uuid("task_id").notNull(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    assigneeId: uuid("assignee_id"),
    assigneeName: varchar("assignee_name", { length: 160 }),
    workShift: varchar("work_shift", { length: 120 }),
    assignedBy: uuid("assigned_by"),
    status: varchar("status", { length: 40 }).default("active").notNull(),
    assignedAt: timestamp("assigned_at").defaultNow(),
    completedAt: timestamp("completed_at"),
});
export const taskDependenciesTable = pgTable("task_dependencies", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    taskId: uuid("task_id").notNull(),
    dependsOnTaskId: uuid("depends_on_task_id").notNull(),
    dependencyType: varchar("dependency_type", { length: 60 }).default("prerequisite").notNull(),
    status: varchar("status", { length: 40 }).default("pending").notNull(),
    createdAt: timestamp("created_at").defaultNow(),
});
export const taskProgressTable = pgTable("task_progress", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    taskId: uuid("task_id").notNull(),
    progressPercent: integer("progress_percent").notNull(),
    completedUnits: integer("completed_units"),
    totalUnits: integer("total_units"),
    comment: text("comment"),
    updatedBy: uuid("updated_by"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const taskStatusHistoryTable = pgTable("task_status_history", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    taskId: uuid("task_id").notNull(),
    oldStatus: varchar("old_status", { length: 40 }),
    newStatus: varchar("new_status", { length: 40 }).notNull(),
    reason: text("reason"),
    changedBy: uuid("changed_by"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const taskNotesTable = pgTable("task_notes", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    taskId: uuid("task_id").notNull(),
    noteType: varchar("note_type", { length: 40 }).default("operational").notNull(),
    note: text("note").notNull(),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const taskAttachmentsTable = pgTable("task_attachments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").notNull(),
    taskId: uuid("task_id").notNull(),
    fileName: varchar("file_name", { length: 255 }).notNull(),
    fileUrl: text("file_url").notNull(),
    fileType: varchar("file_type", { length: 120 }),
    fileSize: integer("file_size"),
    metadata: jsonb("metadata").default({}).notNull(),
    uploadedBy: uuid("uploaded_by"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const putawayRuleApplicationsTable = pgTable("putaway_rule_applications", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    ruleId: uuid("rule_id").references(() => putawayRulesTable.id),
    sku: varchar("sku"),
    matchedZoneId: uuid("matched_zone_id").references(() => warehouseZonesTable.id),
    matchedBinId: uuid("matched_bin_id").references(() => binLocationsTable.id),
    status: varchar("status").notNull(),
    errorMessage: text("error_message"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const productCategoriesTable = pgTable("product_categories", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    parentCategoryId: uuid("parent_category_id").references(() => productCategoriesTable.id),
    categoryCode: varchar("category_code"),
    categoryName: varchar("category_name"),
    description: text("description"),
    sortOrder: integer("sort_order"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const brandsTable = pgTable("brands", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    brandCode: varchar("brand_code"),
    brandName: varchar("brand_name"),
    description: text("description"),
    website: varchar("website"),
    createdAt: timestamp("created_at"),
});
export const unitsOfMeasureTable = pgTable("units_of_measure", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    uomCode: varchar("uom_code"),
    uomName: varchar("uom_name"),
    uomCategory: varchar("uom_category"),
    decimalPrecision: integer("decimal_precision"),
    createdAt: timestamp("created_at"),
});
export const unitConversionsTable = pgTable("unit_conversions", {
    id: uuid("id").primaryKey(),
    fromUomId: uuid("from_uom_id").references(() => unitsOfMeasureTable.id),
    toUomId: uuid("to_uom_id").references(() => unitsOfMeasureTable.id),
    conversionFactor: numeric("conversion_factor"),
});
export const productsTable = pgTable("products", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    productCode: varchar("product_code"),
    categoryId: uuid("category_id").references(() => productCategoriesTable.id),
    brandId: uuid("brand_id").references(() => brandsTable.id),
    defaultUomId: uuid("default_uom_id").references(() => unitsOfMeasureTable.id),
    sku: varchar("sku").unique(),
    barcode: varchar("barcode"),
    qrCode: varchar("qr_code"),
    productName: varchar("product_name"),
    shortDescription: text("short_description"),
    longDescription: text("long_description"),
    productType: varchar("product_type"),
    productStatus: varchar("product_status"),
    lifecycleStatus: varchar("lifecycle_status"),
    productCondition: varchar("product_condition"),
    weight: numeric("weight"),
    length: numeric("length"),
    width: numeric("width"),
    height: numeric("height"),
    volume: numeric("volume"),
    reorderLevel: numeric("reorder_level"),
    minimumStockLevel: numeric("minimum_stock_level"),
    maximumStockLevel: numeric("maximum_stock_level"),
    reorderQuantity: numeric("reorder_quantity"),
    leadTimeDays: integer("lead_time_days"),
    shelfLifeDays: integer("shelf_life_days"),
    supportsSerialTracking: boolean("supports_serial_tracking"),
    supportsBatchTracking: boolean("supports_batch_tracking"),
    supportsExpiryTracking: boolean("supports_expiry_tracking"),
    supportsNegativeStock: boolean("supports_negative_stock"),
    costingMethod: varchar("costing_method"),
    standardCost: numeric("standard_cost"),
    averageCost: numeric("average_cost"),
    sellingPrice: numeric("selling_price"),
    taxRuleId: uuid("tax_rule_id"),
    preferredSupplierId: uuid("preferred_supplier_id"),
    searchMetadata: jsonb("search_metadata"),
    tags: jsonb("tags"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
    deletedAt: timestamp("deleted_at"),
    version: integer("version"),
});
export const productVariantsTable = pgTable("product_variants", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    productId: uuid("product_id").references(() => productsTable.id),
    variantCode: varchar("variant_code"),
    variantName: varchar("variant_name"),
    variantAttributes: jsonb("variant_attributes"),
    color: varchar("color"),
    size: varchar("size"),
    material: varchar("material"),
    style: varchar("style"),
    barcode: varchar("barcode"),
    sku: varchar("sku"),
    skuStatus: varchar("sku_status"),
    qrCode: varchar("qr_code"),
    qrIdentifier: varchar("qr_identifier"),
    qrPayload: text("qr_payload"),
    qrChecksum: varchar("qr_checksum"),
    supplierMappings: jsonb("supplier_mappings"),
    inventoryProfile: jsonb("inventory_profile"),
    warehouseProfile: jsonb("warehouse_profile"),
    taxProfile: jsonb("tax_profile"),
    weight: numeric("weight"),
    costPrice: numeric("cost_price"),
    sellingPrice: numeric("selling_price"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
    deletedAt: timestamp("deleted_at"),
    version: integer("version"),
});
export const productBundleItemsTable = pgTable("product_bundle_items", {
    id: uuid("id").primaryKey(),
    parentProductId: uuid("parent_product_id").references(() => productsTable.id),
    childProductId: uuid("child_product_id").references(() => productsTable.id),
    quantity: numeric("quantity"),
});
export const productMediaTable = pgTable("product_media", {
    id: uuid("id").primaryKey(),
    productId: uuid("product_id").references(() => productsTable.id),
    mediaType: varchar("media_type"),
    fileUrl: text("file_url"),
    fileName: varchar("file_name"),
    fileSize: bigint("file_size", { mode: "number" }),
    mimeType: varchar("mime_type"),
    isPrimary: boolean("is_primary"),
    createdAt: timestamp("created_at"),
});
export const productTagsTable = pgTable("product_tags", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    tagName: varchar("tag_name"),
});
export const productTagMappingsTable = pgTable("product_tag_mappings", {
    id: uuid("id").primaryKey(),
    productId: uuid("product_id").references(() => productsTable.id),
    productTagId: uuid("product_tag_id").references(() => productTagsTable.id),
});
export const productCustomFieldsTable = pgTable("product_custom_fields", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    entityType: varchar("entity_type"),
    fieldName: varchar("field_name"),
    fieldLabel: varchar("field_label"),
    fieldType: varchar("field_type"),
    isRequired: boolean("is_required"),
    defaultValue: varchar("default_value"),
});
export const productCustomFieldValuesTable = pgTable("product_custom_field_values", {
    id: uuid("id").primaryKey(),
    productId: uuid("product_id").references(() => productsTable.id),
    customFieldId: uuid("custom_field_id").references(() => productCustomFieldsTable.id),
    fieldValue: text("field_value"),
});
export const stockItemsTable = pgTable("stock_items", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    productId: uuid("product_id").references(() => productsTable.id),
    productVariantId: uuid("product_variant_id").references(() => productVariantsTable.id),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    warehouseZoneId: uuid("warehouse_zone_id").references(() => warehouseZonesTable.id),
    binLocationId: uuid("bin_location_id").references(() => binLocationsTable.id),
    quantityOnHand: numeric("quantity_on_hand"),
    quantityReserved: numeric("quantity_reserved"),
    quantityAvailable: numeric("quantity_available"),
    quantityInTransit: numeric("quantity_in_transit"),
    quantityOnOrder: numeric("quantity_on_order"),
    quantityAllocated: numeric("quantity_allocated"),
    quantityPicked: numeric("quantity_picked"),
    quantityPacked: numeric("quantity_packed"),
    quantityShipped: numeric("quantity_shipped"),
    quantityReceived: numeric("quantity_received"),
    quantityDamaged: numeric("quantity_damaged"),
    quantityQuarantine: numeric("quantity_quarantine"),
    quantityReturned: numeric("quantity_returned"),
    quantityBlocked: numeric("quantity_blocked"),
    averageCost: numeric("average_cost"),
    lastCountedAt: timestamp("last_counted_at"),
    lastMovementAt: timestamp("last_movement_at"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
    version: integer("version"),
});
export const inventorySnapshotsTable = pgTable("inventory_snapshots", {
    id: uuid("id").primaryKey(),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    snapshotDate: date("snapshot_date"),
    quantityOnHand: numeric("quantity_on_hand"),
    quantityReserved: numeric("quantity_reserved"),
    averageCost: numeric("average_cost"),
    inventoryValue: numeric("inventory_value"),
});
export const stockMovementsTable = pgTable("stock_movements", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    transactionNumber: varchar("transaction_number"),
    movementType: varchar("movement_type"),
    referenceType: varchar("reference_type"),
    referenceId: uuid("reference_id"),
    sourceWarehouseId: uuid("source_warehouse_id").references(() => warehousesTable.id),
    destinationWarehouseId: uuid("destination_warehouse_id").references(() => warehousesTable.id),
    sourceBinId: uuid("source_bin_id").references(() => binLocationsTable.id),
    destinationBinId: uuid("destination_bin_id").references(() => binLocationsTable.id),
    quantity: numeric("quantity"),
    quantityBefore: numeric("quantity_before"),
    quantityAfter: numeric("quantity_after"),
    unitCost: numeric("unit_cost"),
    totalCost: numeric("total_cost"),
    movementReason: varchar("movement_reason"),
    movementDate: timestamp("movement_date"),
    performedBy: uuid("performed_by"),
    idempotencyKey: varchar("idempotency_key"),
    metadata: jsonb("metadata"),
    createdAt: timestamp("created_at"),
});
export const barcodeScansTable = pgTable("barcode_scans", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    scannedCode: varchar("scanned_code"),
    resolvedEntityType: varchar("resolved_entity_type"),
    resolvedEntityId: uuid("resolved_entity_id"),
    warehouseId: uuid("warehouse_id"),
    scannerType: varchar("scanner_type"),
    scannerDeviceId: varchar("scanner_device_id"),
    scanAction: varchar("scan_action"),
    status: varchar("status"),
    failureReason: text("failure_reason"),
    scannedBy: uuid("scanned_by"),
    metadata: jsonb("metadata"),
    createdAt: timestamp("created_at"),
});
export const stockReservationsTable = pgTable("stock_reservations", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    salesOrderId: uuid("sales_order_id"),
    salesOrderItemId: uuid("sales_order_item_id"),
    sourceType: varchar("source_type"),
    sourceId: varchar("source_id"),
    sourceItemId: varchar("source_item_id"),
    idempotencyKey: varchar("idempotency_key"),
    reservedQuantity: numeric("reserved_quantity"),
    allocatedQuantity: numeric("allocated_quantity"),
    fulfilledQuantity: numeric("fulfilled_quantity"),
    reservationStatus: varchar("reservation_status"),
    reservedUntil: timestamp("reserved_until"),
    releasedAt: timestamp("released_at"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
    version: integer("version"),
    reservationName: varchar("reservation_name"),
    description: text("description"),
    priority: varchar("priority"),
    releaseAfterDate: timestamp("release_after_date"),
    allocationStrategy: varchar("allocation_strategy"),
    allowPartialAllocation: boolean("allow_partial_allocation"),
    holdForFuture: boolean("hold_for_future"),
    requireApproval: boolean("require_approval"),
    notifyOnExpiry: boolean("notify_on_expiry"),
    notes: text("notes"),
});
export const stockAdjustmentsTable = pgTable("stock_adjustments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    adjustmentNumber: varchar("adjustment_number"),
    idempotencyKey: varchar("idempotency_key"),
    adjustmentType: varchar("adjustment_type"),
    reason: varchar("reason"),
    notes: text("notes"),
    status: varchar("status"),
    approvalRequestId: uuid("approval_request_id"),
    approvalThreshold: numeric("approval_threshold"),
    evidence: jsonb("evidence"),
    approvedBy: uuid("approved_by"),
    approvedAt: timestamp("approved_at"),
    rejectedBy: uuid("rejected_by"),
    rejectedAt: timestamp("rejected_at"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
    version: integer("version"),
    // Extended fields
    referenceNumber: varchar("reference_number"),
    department: varchar("department"),
    reasonCode: varchar("reason_code"),
    reasonDescription: text("reason_description"),
    adjustmentAccount: varchar("adjustment_account"),
    financialImpactEstimate: numeric("financial_impact_estimate"),
    tags: jsonb("tags"),
    submittedAt: timestamp("submitted_at"),
    cancelledAt: timestamp("cancelled_at"),
    completedAt: timestamp("completed_at"),
    adjustmentDate: timestamp("adjustment_date"),
    locationId: uuid("location_id").references(() => binLocationsTable.id),
    approvalRequired: boolean("approval_required").default(false),
    workflowDefinitionId: uuid("workflow_definition_id"),
    nextApproverUserId: uuid("next_approver_user_id"),
    ipAddress: varchar("ip_address"),
});
export const stockAdjustmentItemsTable = pgTable("stock_adjustment_items", {
    id: uuid("id").primaryKey(),
    stockAdjustmentId: uuid("stock_adjustment_id").references(() => stockAdjustmentsTable.id),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    quantityBefore: numeric("quantity_before"),
    quantityAfter: numeric("quantity_after"),
    adjustedQuantity: numeric("adjusted_quantity"),
    unitCost: numeric("unit_cost"),
    reason: varchar("reason"),
    evidence: jsonb("evidence"),
    // Extended fields
    productId: uuid("product_id").references(() => productsTable.id),
    productVariantId: uuid("product_variant_id").references(() => productVariantsTable.id),
    lotNumber: varchar("lot_number"),
    batchNumber: varchar("batch_number"),
    serialNumber: varchar("serial_number"),
    binLocationId: uuid("bin_location_id").references(() => binLocationsTable.id),
    uomId: uuid("uom_id").references(() => unitsOfMeasureTable.id),
    totalValue: numeric("total_value"),
    onHandQuantity: numeric("on_hand_quantity"),
    expiryDate: timestamp("expiry_date"),
});
export const stockAdjustmentAttachmentsTable = pgTable("stock_adjustment_attachments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockAdjustmentId: uuid("stock_adjustment_id").references(() => stockAdjustmentsTable.id),
    fileName: varchar("file_name").notNull(),
    fileSize: integer("file_size").notNull(),
    contentType: varchar("content_type").notNull(),
    fileUrl: text("file_url").notNull(),
    uploadedBy: uuid("uploaded_by"),
    uploadedAt: timestamp("uploaded_at").defaultNow(),
});
export const stockAdjustmentCommentsTable = pgTable("stock_adjustment_comments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockAdjustmentId: uuid("stock_adjustment_id").references(() => stockAdjustmentsTable.id),
    commentText: text("comment_text").notNull(),
    commentType: varchar("comment_type").default("internal"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const stockAdjustmentApprovalsTable = pgTable("stock_adjustment_approvals", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockAdjustmentId: uuid("stock_adjustment_id").references(() => stockAdjustmentsTable.id),
    approverUserId: uuid("approver_user_id"),
    approvalStep: integer("approval_step").default(1),
    status: varchar("status").notNull(),
    comments: text("comments"),
    reassignedToUserId: uuid("reassigned_to_user_id"),
    decidedAt: timestamp("decided_at"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at"),
});
export const stockAdjustmentAuditLogsTable = pgTable("stock_adjustment_audit_logs", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockAdjustmentId: uuid("stock_adjustment_id").references(() => stockAdjustmentsTable.id),
    userId: uuid("user_id"),
    action: varchar("action").notNull(),
    previousState: jsonb("previous_state"),
    newState: jsonb("new_state"),
    ipAddress: varchar("ip_address"),
    userAgent: varchar("user_agent"),
    comments: text("comments"),
    timestamp: timestamp("timestamp").defaultNow(),
});
export const stockTransfersTable = pgTable("stock_transfers", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    sourceWarehouseId: uuid("source_warehouse_id").references(() => warehousesTable.id),
    destinationWarehouseId: uuid("destination_warehouse_id").references(() => warehousesTable.id),
    transferNumber: varchar("transfer_number"),
    idempotencyKey: varchar("idempotency_key"),
    transferStatus: varchar("transfer_status"),
    sourceBinId: uuid("source_bin_id").references(() => binLocationsTable.id),
    destinationBinId: uuid("destination_bin_id").references(() => binLocationsTable.id),
    approvalRequestId: uuid("approval_request_id"),
    requestedAt: timestamp("requested_at"),
    approvedBy: uuid("approved_by"),
    approvedAt: timestamp("approved_at"),
    pickedAt: timestamp("picked_at"),
    shippedAt: timestamp("shipped_at"),
    receivedAt: timestamp("received_at"),
    completedAt: timestamp("completed_at"),
    cancelledAt: timestamp("cancelled_at"),
    notes: text("notes"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
    version: integer("version"),
    priority: varchar("priority"),
    sourceType: varchar("source_type"),
    destinationType: varchar("destination_type"),
    reason: varchar("reason"),
    department: varchar("department"),
    requestedByRole: varchar("requested_by_role"),
});
export const stockTransferItemsTable = pgTable("stock_transfer_items", {
    id: uuid("id").primaryKey(),
    stockTransferId: uuid("stock_transfer_id").references(() => stockTransfersTable.id),
    productId: uuid("product_id").references(() => productsTable.id),
    productVariantId: uuid("product_variant_id").references(() => productVariantsTable.id),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    destinationStockItemId: uuid("destination_stock_item_id").references(() => stockItemsTable.id),
    quantity: numeric("quantity"),
    pickedQuantity: numeric("picked_quantity"),
    receivedQuantity: numeric("received_quantity"),
    lotNumber: varchar("lot_number"),
    batchNumber: varchar("batch_number"),
    serialNumber: varchar("serial_number"),
    sourceBinId: uuid("source_bin_id").references(() => binLocationsTable.id),
    destinationBinId: uuid("destination_bin_id").references(() => binLocationsTable.id),
    unitCost: numeric("unit_cost"),
    approvedQuantity: numeric("approved_quantity"),
    shippedQuantity: numeric("shipped_quantity"),
    rejectedQuantity: numeric("rejected_quantity"),
    varianceQuantity: numeric("variance_quantity"),
    lineStatus: varchar("line_status"),
    lineValidationStatus: varchar("line_validation_status"),
    lineValidationMessage: text("line_validation_message"),
});
export const stockTransferApprovalsTable = pgTable("stock_transfer_approvals", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockTransferId: uuid("stock_transfer_id").references(() => stockTransfersTable.id),
    approverId: uuid("approver_id"),
    approverName: varchar("approver_name"),
    approverRole: varchar("approver_role"),
    decision: varchar("decision").notNull(),
    comments: text("comments"),
    decisionTimestamp: timestamp("decision_timestamp").defaultNow(),
    createdAt: timestamp("created_at").defaultNow(),
});
export const stockTransferAttachmentsTable = pgTable("stock_transfer_attachments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockTransferId: uuid("stock_transfer_id").references(() => stockTransfersTable.id),
    fileName: varchar("file_name").notNull(),
    fileType: varchar("file_type").notNull(),
    fileSize: integer("file_size").notNull(),
    fileUrl: varchar("file_url").notNull(),
    uploadedBy: uuid("uploaded_by"),
    uploadedByName: varchar("uploaded_by_name"),
    uploadedDate: timestamp("uploaded_date").defaultNow(),
    createdAt: timestamp("created_at").defaultNow(),
});
export const stockTransferTimelineTable = pgTable("stock_transfer_timeline", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockTransferId: uuid("stock_transfer_id").references(() => stockTransfersTable.id),
    timestamp: timestamp("timestamp").defaultNow(),
    userId: uuid("user_id"),
    userName: varchar("user_name"),
    action: varchar("action").notNull(),
    comments: text("comments"),
    status: varchar("status"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const stockTransferReservationsTable = pgTable("stock_transfer_reservations", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockTransferId: uuid("stock_transfer_id").references(() => stockTransfersTable.id),
    stockTransferItemId: uuid("stock_transfer_item_id").references(() => stockTransferItemsTable.id),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    quantity: numeric("quantity").notNull(),
    status: varchar("status").notNull(),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at"),
});
export const stockTransferReceiptsTable = pgTable("stock_transfer_receipts", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockTransferId: uuid("stock_transfer_id").references(() => stockTransfersTable.id),
    receivedBy: uuid("received_by"),
    receivedByName: varchar("received_by_name"),
    receivedAt: timestamp("received_at").defaultNow(),
    status: varchar("status").notNull(),
    notes: text("notes"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const stockTransferRoutesTable = pgTable("stock_transfer_routes", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockTransferId: uuid("stock_transfer_id").references(() => stockTransfersTable.id),
    sourceZone: varchar("source_zone"),
    sourceLocation: varchar("source_location"),
    sourceBin: varchar("source_bin"),
    destinationZone: varchar("destination_zone"),
    destinationLocation: varchar("destination_location"),
    destinationBin: varchar("destination_bin"),
    estimatedTransitDuration: varchar("estimated_transit_duration"),
    carrier: varchar("carrier"),
    logisticsProvider: varchar("logistics_provider"),
    movementMethod: varchar("movement_method"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at"),
});
export const stockTransferRiskAssessmentsTable = pgTable("stock_transfer_risk_assessments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockTransferId: uuid("stock_transfer_id").references(() => stockTransfersTable.id),
    sourceAvailableQty: numeric("source_available_qty"),
    transferQty: numeric("transfer_qty"),
    sourceValidationStatus: varchar("source_validation_status"),
    destinationCapacity: numeric("destination_capacity"),
    availableCapacity: numeric("available_capacity"),
    projectedCapacity: numeric("projected_capacity"),
    destinationValidationStatus: varchar("destination_validation_status"),
    transitDistance: numeric("transit_distance"),
    estimatedTransitTime: varchar("estimated_transit_time"),
    transitRiskLevel: varchar("transit_risk_level"),
    accuracyPercentage: numeric("accuracy_percentage"),
    historicalPerformance: varchar("historical_performance"),
    overallAssessment: varchar("overall_assessment"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at"),
});
export const stockTransferRouteAssessmentsTable = pgTable("stock_transfer_route_assessments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    stockTransferId: uuid("stock_transfer_id").references(() => stockTransfersTable.id),
    routeScore: numeric("route_score"),
    riskFactor: varchar("risk_factor"),
    mitigationSteps: text("mitigation_steps"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at"),
});
export const reorderRulesTable = pgTable("reorder_rules", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    organizationId: uuid("organization_id"),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    productId: uuid("product_id").references(() => productsTable.id),
    skuId: uuid("sku_id").references(() => productVariantsTable.id),
    ruleScope: varchar("rule_scope", { length: 32 }).notNull(),
    minimumStock: numeric("minimum_stock").notNull().default("0"),
    maximumStock: numeric("maximum_stock"),
    reorderPoint: numeric("reorder_point").notNull().default("0"),
    reorderQuantity: numeric("reorder_quantity").notNull().default("0"),
    safetyStock: numeric("safety_stock"),
    leadTimeDays: integer("lead_time_days"),
    supplierLeadTimeDays: integer("supplier_lead_time_days"),
    preferredSupplierId: uuid("preferred_supplier_id").references(() => suppliersTable.id),
    reviewMode: varchar("review_mode", { length: 32 }).notNull().default("manual"),
    isActive: boolean("is_active").notNull().default(true),
    metadata: jsonb("metadata").notNull().default({}),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at").notNull().defaultNow(),
    updatedAt: timestamp("updated_at").notNull().defaultNow(),
    version: integer("version").notNull().default(1),
});
export const reorderRecommendationsTable = pgTable("reorder_recommendations", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    ruleId: uuid("rule_id").references(() => reorderRulesTable.id),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    stockItemId: uuid("stock_item_id")
        .notNull()
        .references(() => stockItemsTable.id),
    skuId: uuid("sku_id").references(() => productVariantsTable.id),
    productId: uuid("product_id").references(() => productsTable.id),
    preferredSupplierId: uuid("preferred_supplier_id").references(() => suppliersTable.id),
    currentAvailable: numeric("current_available").notNull().default("0"),
    currentOnHand: numeric("current_on_hand").notNull().default("0"),
    currentReserved: numeric("current_reserved").notNull().default("0"),
    incomingQuantity: numeric("incoming_quantity").notNull().default("0"),
    outgoingQuantity: numeric("outgoing_quantity").notNull().default("0"),
    reorderPoint: numeric("reorder_point").notNull().default("0"),
    recommendedQuantity: numeric("recommended_quantity").notNull().default("0"),
    riskLevel: varchar("risk_level", { length: 32 }).notNull(),
    status: varchar("status", { length: 64 }).notNull().default("pending_review"),
    reason: text("reason").notNull(),
    createdAt: timestamp("created_at").notNull().defaultNow(),
    updatedAt: timestamp("updated_at").notNull().defaultNow(),
    version: integer("version").notNull().default(1),
});
export const batchesTable = pgTable("batches", {
    id: uuid("id").primaryKey(),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    batchNumber: varchar("batch_number"),
    manufactureDate: date("manufacture_date"),
    expiryDate: date("expiry_date"),
    receivedDate: date("received_date"),
    quantity: numeric("quantity"),
    status: varchar("status"),
});
export const serialNumbersTable = pgTable("serial_numbers", {
    id: uuid("id").primaryKey(),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    serialNumber: varchar("serial_number"),
    batchId: uuid("batch_id").references(() => batchesTable.id),
    status: varchar("status"),
    warrantyStartDate: date("warranty_start_date"),
    warrantyEndDate: date("warranty_end_date"),
});
export const suppliersTable = pgTable("suppliers", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    supplierCode: varchar("supplier_code"),
    supplierName: varchar("supplier_name"),
    supplierType: varchar("supplier_type"),
    email: varchar("email"),
    phone: varchar("phone"),
    website: varchar("website"),
    taxNumber: varchar("tax_number"),
    paymentTerms: varchar("payment_terms"),
    creditLimit: numeric("credit_limit"),
    rating: numeric("rating"),
    status: varchar("status"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const supplierContactsTable = pgTable("supplier_contacts", {
    id: uuid("id").primaryKey(),
    supplierId: uuid("supplier_id").references(() => suppliersTable.id),
    contactName: varchar("contact_name"),
    designation: varchar("designation"),
    email: varchar("email"),
    phone: varchar("phone"),
    isPrimary: boolean("is_primary"),
});
export const supplierPriceListsTable = pgTable("supplier_price_lists", {
    id: uuid("id").primaryKey(),
    supplierId: uuid("supplier_id").references(() => suppliersTable.id),
    productId: uuid("product_id").references(() => productsTable.id),
    minimumQuantity: numeric("minimum_quantity"),
    unitCost: numeric("unit_cost"),
    currencyCode: varchar("currency_code"),
    leadTimeDays: integer("lead_time_days"),
    effectiveFrom: date("effective_from"),
    effectiveTo: date("effective_to"),
});
export const supplierDocumentsTable = pgTable("supplier_documents", {
    id: uuid("id").primaryKey(),
    supplierId: uuid("supplier_id").references(() => suppliersTable.id),
    documentType: varchar("document_type"),
    fileName: varchar("file_name"),
    fileKey: varchar("file_key"),
    fileSize: bigint("file_size", { mode: "number" }),
    mimeType: varchar("mime_type"),
    uploadedBy: uuid("uploaded_by"),
    uploadedAt: timestamp("uploaded_at"),
    expiresAt: timestamp("expires_at"),
    isArchived: boolean("is_archived"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const supplierPerformanceTable = pgTable("supplier_performance", {
    id: uuid("id").primaryKey(),
    supplierId: uuid("supplier_id").references(() => suppliersTable.id),
    deliveryDelayDays: numeric("delivery_delay_days"),
    qualityScore: numeric("quality_score"),
    returnRate: numeric("return_rate"),
    orderFulfillmentAccuracy: numeric("order_fulfillment_accuracy"),
    overallRating: numeric("overall_rating"),
    lastAssessmentDate: timestamp("last_assessment_date"),
    updatedAt: timestamp("updated_at"),
});
export const supplierNotesTable = pgTable("supplier_notes", {
    id: uuid("id").primaryKey(),
    supplierId: uuid("supplier_id").references(() => suppliersTable.id),
    content: text("content"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const supplierPaymentTermsTable = pgTable("supplier_payment_terms", {
    id: uuid("id").primaryKey(),
    supplierId: uuid("supplier_id").references(() => suppliersTable.id),
    termName: varchar("term_name"),
    daysDue: integer("days_due"),
    discountPercentage: numeric("discount_percentage"),
    discountDays: integer("discount_days"),
    isDefault: boolean("is_default"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const supplierCategoriesTable = pgTable("supplier_categories", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    categoryCode: varchar("category_code"),
    categoryName: varchar("category_name"),
    description: text("description"),
    isActive: boolean("is_active"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const purchaseOrdersTable = pgTable("purchase_orders", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    supplierId: uuid("supplier_id").references(() => suppliersTable.id),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    purchaseOrderNumber: varchar("purchase_order_number"),
    orderDate: date("order_date"),
    expectedDeliveryDate: date("expected_delivery_date"),
    status: varchar("status"),
    subtotal: numeric("subtotal"),
    taxAmount: numeric("tax_amount"),
    shippingAmount: numeric("shipping_amount"),
    discountAmount: numeric("discount_amount"),
    totalAmount: numeric("total_amount"),
    currencyCode: varchar("currency_code"),
    approvedBy: uuid("approved_by"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
});
export const purchaseOrderItemsTable = pgTable("purchase_order_items", {
    id: uuid("id").primaryKey(),
    purchaseOrderId: uuid("purchase_order_id").references(() => purchaseOrdersTable.id),
    productId: uuid("product_id").references(() => productsTable.id),
    productVariantId: uuid("product_variant_id").references(() => productVariantsTable.id),
    quantityOrdered: numeric("quantity_ordered"),
    quantityReceived: numeric("quantity_received"),
    unitCost: numeric("unit_cost"),
    taxAmount: numeric("tax_amount"),
    discountAmount: numeric("discount_amount"),
    lineTotal: numeric("line_total"),
});
export const goodsReceiptsTable = pgTable("goods_receipts", {
    id: uuid("id").primaryKey(),
    purchaseOrderId: uuid("purchase_order_id").references(() => purchaseOrdersTable.id),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    goodsReceiptNumber: varchar("goods_receipt_number"),
    receivedBy: uuid("received_by"),
    receivedAt: timestamp("received_at"),
    receiptStatus: varchar("receipt_status"),
});
export const goodsReceiptItemsTable = pgTable("goods_receipt_items", {
    id: uuid("id").primaryKey(),
    goodsReceiptId: uuid("goods_receipt_id").references(() => goodsReceiptsTable.id),
    purchaseOrderItemId: uuid("purchase_order_item_id").references(() => purchaseOrderItemsTable.id),
    quantityReceived: numeric("quantity_received"),
    quantityRejected: numeric("quantity_rejected"),
    batchNumber: varchar("batch_number"),
    expiryDate: date("expiry_date"),
});
export const customersTable = pgTable("customers", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    customerCode: varchar("customer_code"),
    customerName: varchar("customer_name"),
    customerType: varchar("customer_type"),
    email: varchar("email"),
    phone: varchar("phone"),
    taxNumber: varchar("tax_number"),
    customerGroupId: uuid("customer_group_id"),
    creditLimit: numeric("credit_limit"),
    paymentTerms: varchar("payment_terms"),
    preferredCurrencyCode: varchar("preferred_currency_code"),
    status: varchar("status"),
    createdAt: timestamp("created_at"),
});
export const customerAddressesTable = pgTable("customer_addresses", {
    id: uuid("id").primaryKey(),
    customerId: uuid("customer_id").references(() => customersTable.id),
    addressType: varchar("address_type"),
    line1: varchar("line1"),
    line2: varchar("line2"),
    city: varchar("city"),
    state: varchar("state"),
    postalCode: varchar("postal_code"),
    country: varchar("country"),
});
export const salesOrdersTable = pgTable("sales_orders", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    customerId: uuid("customer_id").references(() => customersTable.id),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    salesOrderNumber: varchar("sales_order_number"),
    orderType: varchar("order_type"),
    orderDate: date("order_date"),
    promisedDeliveryDate: date("promised_delivery_date"),
    status: varchar("status"),
    subtotal: numeric("subtotal"),
    taxAmount: numeric("tax_amount"),
    shippingAmount: numeric("shipping_amount"),
    discountAmount: numeric("discount_amount"),
    totalAmount: numeric("total_amount"),
    currencyCode: varchar("currency_code"),
    createdBy: uuid("created_by"),
    approvedBy: uuid("approved_by"),
    createdAt: timestamp("created_at"),
});
export const salesOrderItemsTable = pgTable("sales_order_items", {
    id: uuid("id").primaryKey(),
    salesOrderId: uuid("sales_order_id").references(() => salesOrdersTable.id),
    productId: uuid("product_id").references(() => productsTable.id),
    productVariantId: uuid("product_variant_id").references(() => productVariantsTable.id),
    quantityOrdered: numeric("quantity_ordered"),
    quantityAllocated: numeric("quantity_allocated"),
    quantityShipped: numeric("quantity_shipped"),
    quantityReturned: numeric("quantity_returned"),
    unitPrice: numeric("unit_price"),
    taxAmount: numeric("tax_amount"),
    discountAmount: numeric("discount_amount"),
    lineTotal: numeric("line_total"),
});
export const shipmentsTable = pgTable("shipments", {
    id: uuid("id").primaryKey(),
    salesOrderId: uuid("sales_order_id").references(() => salesOrdersTable.id),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    shipmentNumber: varchar("shipment_number"),
    carrierName: varchar("carrier_name"),
    serviceLevel: varchar("service_level"),
    trackingNumber: varchar("tracking_number"),
    shipmentStatus: varchar("shipment_status"),
    shippedAt: timestamp("shipped_at"),
    deliveredAt: timestamp("delivered_at"),
});
export const shipmentItemsTable = pgTable("shipment_items", {
    id: uuid("id").primaryKey(),
    shipmentId: uuid("shipment_id").references(() => shipmentsTable.id),
    salesOrderItemId: uuid("sales_order_item_id").references(() => salesOrderItemsTable.id),
    quantityShipped: numeric("quantity_shipped"),
});
export const invoicesTable = pgTable("invoices", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    organizationId: uuid("organization_id"),
    invoiceType: varchar("invoice_type"),
    salesOrderId: uuid("sales_order_id").references(() => salesOrdersTable.id),
    customerId: uuid("customer_id").references(() => customersTable.id),
    supplierId: uuid("supplier_id"),
    purchaseOrderId: uuid("purchase_order_id").references(() => purchaseOrdersTable.id),
    goodsReceiptId: uuid("goods_receipt_id"),
    invoiceNumber: varchar("invoice_number"),
    invoiceDate: date("invoice_date"),
    dueDate: date("due_date"),
    currencyCode: varchar("currency_code"),
    subtotal: numeric("subtotal"),
    taxAmount: numeric("tax_amount"),
    discountAmount: numeric("discount_amount"),
    totalAmount: numeric("total_amount"),
    paidAmount: numeric("paid_amount"),
    outstandingAmount: numeric("outstanding_amount"),
    invoiceStatus: varchar("invoice_status"),
    notes: text("notes"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const invoiceLineItemsTable = pgTable("invoice_line_items", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    invoiceId: uuid("invoice_id").references(() => invoicesTable.id),
    productId: uuid("product_id").references(() => productsTable.id),
    productVariantId: uuid("product_variant_id").references(() => productVariantsTable.id),
    description: text("description"),
    quantity: numeric("quantity"),
    unitPrice: numeric("unit_price"),
    discountAmount: numeric("discount_amount"),
    taxType: varchar("tax_type"),
    taxRate: numeric("tax_rate"),
    taxAmount: numeric("tax_amount"),
    lineTotal: numeric("line_total"),
    createdAt: timestamp("created_at"),
});
export const paymentsTable = pgTable("payments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    invoiceId: uuid("invoice_id").references(() => invoicesTable.id),
    paymentMethod: varchar("payment_method"),
    paymentReference: varchar("payment_reference"),
    currencyCode: varchar("currency_code"),
    amount: numeric("amount"),
    paymentStatus: varchar("payment_status"),
    paidAt: timestamp("paid_at"),
    recordedBy: uuid("recorded_by"),
    createdAt: timestamp("created_at"),
});
export const paymentAllocationsTable = pgTable("payment_allocations", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    paymentId: uuid("payment_id").references(() => paymentsTable.id),
    invoiceId: uuid("invoice_id").references(() => invoicesTable.id),
    allocatedAmount: numeric("allocated_amount"),
    createdAt: timestamp("created_at"),
});
export const returnRequestsTable = pgTable("return_requests", {
    id: uuid("id").primaryKey(),
    salesOrderId: uuid("sales_order_id").references(() => salesOrdersTable.id),
    customerId: uuid("customer_id").references(() => customersTable.id),
    returnNumber: varchar("return_number"),
    returnType: varchar("return_type"),
    returnReason: varchar("return_reason"),
    returnStatus: varchar("return_status"),
    requestedAt: timestamp("requested_at"),
});
export const returnItemsTable = pgTable("return_items", {
    id: uuid("id").primaryKey(),
    returnRequestId: uuid("return_request_id").references(() => returnRequestsTable.id),
    salesOrderItemId: uuid("sales_order_item_id").references(() => salesOrderItemsTable.id),
    quantityReturned: numeric("quantity_returned"),
    dispositionAction: varchar("disposition_action"),
});
export const qualityInspectionsTable = pgTable("quality_inspections", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    organizationId: uuid("organization_id"),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    productId: uuid("product_id").references(() => productsTable.id),
    productVariantId: uuid("product_variant_id").references(() => productVariantsTable.id),
    supplierId: uuid("supplier_id"),
    purchaseOrderId: uuid("purchase_order_id").references(() => purchaseOrdersTable.id),
    goodsReceiptId: uuid("goods_receipt_id"),
    checklistId: uuid("checklist_id"),
    referenceType: varchar("reference_type"),
    referenceId: uuid("reference_id"),
    inspectionType: varchar("inspection_type"),
    inspectionResult: varchar("inspection_result"),
    inventoryQualityStatus: varchar("inventory_quality_status"),
    sampleSize: integer("sample_size"),
    quantityInspected: numeric("quantity_inspected"),
    requiresApproval: boolean("requires_approval"),
    inspectedBy: uuid("inspected_by"),
    inspectedAt: timestamp("inspected_at"),
    notes: text("notes"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const quarantineRecordsTable = pgTable("quarantine_records", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    inspectionId: uuid("inspection_id").references(() => qualityInspectionsTable.id),
    stockItemId: uuid("stock_item_id").references(() => stockItemsTable.id),
    quantity: numeric("quantity"),
    quarantineReason: varchar("quarantine_reason"),
    quarantinedBy: uuid("quarantined_by"),
    quarantinedAt: timestamp("quarantined_at"),
    releasedBy: uuid("released_by"),
    releasedAt: timestamp("released_at"),
    releaseNotes: text("release_notes"),
});
export const qualityRulesTable = pgTable("quality_rules", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    organizationId: uuid("organization_id"),
    scope: varchar("scope"),
    scopeId: uuid("scope_id"),
    inspectionType: varchar("inspection_type"),
    requiresApproval: boolean("requires_approval"),
    isActive: boolean("is_active"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const qualityChecklistsTable = pgTable("quality_checklists", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    organizationId: uuid("organization_id"),
    name: varchar("name"),
    inspectionType: varchar("inspection_type"),
    isActive: boolean("is_active"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
    updatedAt: timestamp("updated_at"),
});
export const qualityChecklistItemsTable = pgTable("quality_checklist_items", {
    id: uuid("id").primaryKey(),
    checklistId: uuid("checklist_id").references(() => qualityChecklistsTable.id),
    label: varchar("label"),
    category: varchar("category"),
    isRequired: boolean("is_required"),
    sortOrder: integer("sort_order"),
    createdAt: timestamp("created_at"),
});
export const qualityDefectsTable = pgTable("quality_defects", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    inspectionId: uuid("inspection_id").references(() => qualityInspectionsTable.id),
    category: varchar("category"),
    severity: varchar("severity"),
    quantityAffected: numeric("quantity_affected"),
    rootCauseNotes: text("root_cause_notes"),
    correctiveAction: text("corrective_action"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
});
export const qualityAttachmentsTable = pgTable("quality_attachments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    inspectionId: uuid("inspection_id").references(() => qualityInspectionsTable.id),
    fileKey: text("file_key"),
    fileName: varchar("file_name"),
    mimeType: varchar("mime_type"),
    fileSize: bigint("file_size", { mode: "number" }),
    documentType: varchar("document_type"),
    uploadedBy: uuid("uploaded_by"),
    createdAt: timestamp("created_at"),
});
export const documentsTable = pgTable("documents", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    entityType: varchar("entity_type"),
    entityId: uuid("entity_id"),
    documentType: varchar("document_type"),
    fileName: varchar("file_name"),
    fileUrl: text("file_url"),
    mimeType: varchar("mime_type"),
    fileSize: bigint("file_size", { mode: "number" }),
    uploadedBy: uuid("uploaded_by"),
    createdAt: timestamp("created_at"),
});
export const journalEntriesTable = pgTable("journal_entries", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    entryNumber: varchar("entry_number"),
    entryDate: date("entry_date"),
    sourceType: varchar("source_type"),
    sourceId: uuid("source_id"),
    currencyCode: varchar("currency_code"),
    totalDebit: numeric("total_debit"),
    totalCredit: numeric("total_credit"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
});
export const journalEntryLinesTable = pgTable("journal_entry_lines", {
    id: uuid("id").primaryKey(),
    journalEntryId: uuid("journal_entry_id").references(() => journalEntriesTable.id),
    accountCode: varchar("account_code"),
    accountName: varchar("account_name"),
    debitAmount: numeric("debit_amount"),
    creditAmount: numeric("credit_amount"),
    description: text("description"),
});
export const chartOfAccountsTable = pgTable("chart_of_accounts", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    accountCode: varchar("account_code"),
    accountName: varchar("account_name"),
    accountType: varchar("account_type"),
    isSystem: boolean("is_system"),
    createdAt: timestamp("created_at"),
});
export const accountingEventsTable = pgTable("accounting_events", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id"),
    eventType: varchar("event_type"),
    sourceType: varchar("source_type"),
    sourceId: uuid("source_id"),
    amount: numeric("amount"),
    status: varchar("status"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at"),
});
export const pickWavesTable = pgTable("pick_waves", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    waveName: varchar("wave_name").notNull(),
    waveCode: varchar("wave_code").notNull(),
    waveType: varchar("wave_type").notNull(),
    priority: varchar("priority").notNull(),
    status: varchar("status").notNull(),
    description: text("description"),
    maxOrders: integer("max_orders"),
    maxUnits: integer("max_units"),
    maxWeight: numeric("max_weight"),
    plannedStart: timestamp("planned_start"),
    plannedEnd: timestamp("planned_end"),
    actualStart: timestamp("actual_start"),
    actualEnd: timestamp("actual_end"),
    estimatedDurationMinutes: integer("estimated_duration_minutes"),
    pickerAssignmentStrategy: varchar("picker_assignment_strategy"),
    sortingMethod: varchar("sorting_method"),
    tasksGenerated: boolean("tasks_generated"),
    pickersNotified: boolean("pickers_notified"),
    waveOptimizationEnabled: boolean("wave_optimization_enabled"),
    batchPickingEnabled: boolean("batch_picking_enabled"),
    autoReplenishmentEnabled: boolean("auto_replenishment_enabled"),
    routeOptimizationCoverage: numeric("route_optimization_coverage"),
    routeOptimizationDistanceKm: numeric("route_optimization_distance_km"),
    routeOptimizationTimeSavedMinutes: integer("route_optimization_time_saved_minutes"),
    createdBy: uuid("created_by"),
    releasedBy: uuid("released_by"),
    completedBy: uuid("completed_by"),
    cancelledBy: uuid("cancelled_by"),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
    releasedAt: timestamp("released_at"),
    completedAt: timestamp("completed_at"),
    cancelledAt: timestamp("cancelled_at"),
});
export const waveOrdersTable = pgTable("wave_orders", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    waveId: uuid("wave_id").references(() => pickWavesTable.id),
    salesOrderId: uuid("sales_order_id").references(() => salesOrdersTable.id),
    assignedAt: timestamp("assigned_at").defaultNow(),
    sequenceOrder: integer("sequence_order"),
});
export const waveZonesTable = pgTable("wave_zones", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    waveId: uuid("wave_id").references(() => pickWavesTable.id),
    warehouseZoneId: uuid("warehouse_zone_id").references(() => warehouseZonesTable.id),
});
export const wavePickerAssignmentsTable = pgTable("wave_picker_assignments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    waveId: uuid("wave_id").references(() => pickWavesTable.id),
    pickerId: uuid("picker_id"),
    assignedAt: timestamp("assigned_at").defaultNow(),
});
export const waveStatusTimelineTable = pgTable("wave_status_timeline", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    waveId: uuid("wave_id").references(() => pickWavesTable.id),
    status: varchar("status").notNull(),
    notes: text("notes"),
    changedAt: timestamp("changed_at").defaultNow(),
    changedBy: uuid("changed_by"),
});
export const waveTemplatesTable = pgTable("wave_templates", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    templateName: varchar("template_name").notNull(),
    templateCode: varchar("template_code").notNull(),
    waveType: varchar("wave_type").notNull(),
    priority: varchar("priority").notNull(),
    description: text("description"),
    maxOrders: integer("max_orders"),
    maxUnits: integer("max_units"),
    maxWeight: numeric("max_weight"),
    pickerAssignmentStrategy: varchar("picker_assignment_strategy"),
    sortingMethod: varchar("sorting_method"),
    tasksGenerated: boolean("tasks_generated"),
    pickersNotified: boolean("pickers_notified"),
    waveOptimizationEnabled: boolean("wave_optimization_enabled"),
    batchPickingEnabled: boolean("batch_picking_enabled"),
    autoReplenishmentEnabled: boolean("auto_replenishment_enabled"),
    isActive: boolean("is_active").default(true),
    createdAt: timestamp("created_at").defaultNow(),
    updatedAt: timestamp("updated_at").defaultNow(),
});
export const pickListsTable = pgTable("pick_lists", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    waveId: uuid("wave_id").references(() => pickWavesTable.id),
    pickListName: varchar("pick_list_name").notNull(),
    pickListCode: varchar("pick_list_code").notNull().unique(),
    orderType: varchar("order_type").notNull(),
    priority: varchar("priority").notNull(),
    status: varchar("status").notNull(),
    description: text("description"),
    pickMethod: varchar("pick_method"),
    allocationStrategy: varchar("allocation_strategy"),
    optimizeRoute: boolean("optimize_route").default(true),
    groupByLocation: boolean("group_by_location").default(true),
    allowShortPicks: boolean("allow_short_picks").default(true),
    requiresApproval: boolean("requires_approval").default(false),
    notifyOnCompletion: boolean("notify_on_completion").default(false),
    dueDate: timestamp("due_date"),
    estPickTime: integer("est_pick_time"),
    estRouteDistance: numeric("est_route_distance"),
    assignedPickerId: uuid("assigned_picker_id"),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at").defaultNow(),
    startedAt: timestamp("started_at"),
    completedAt: timestamp("completed_at"),
    cancelledAt: timestamp("cancelled_at"),
    version: integer("version").default(1),
});
export const pickListOrdersTable = pgTable("pick_list_orders", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    orderId: uuid("order_id").references(() => salesOrdersTable.id),
    orderNo: varchar("order_no").notNull(),
    customerName: varchar("customer_name"),
    orderDate: timestamp("order_date"),
    totalItems: integer("total_items"),
    dueDate: timestamp("due_date"),
    priority: varchar("priority"),
    status: varchar("status"),
});
export const pickListItemsTable = pgTable("pick_list_items", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    orderId: uuid("order_id").references(() => salesOrdersTable.id),
    productId: uuid("product_id").references(() => productVariantsTable.id),
    productName: varchar("product_name"),
    sku: varchar("sku").notNull(),
    imageUrl: varchar("image_url"),
    quantityOrdered: numeric("quantity_ordered").notNull(),
    quantityPicked: numeric("quantity_picked").default("0"),
    quantityRemaining: numeric("quantity_remaining").notNull(),
    status: varchar("status").notNull(),
    binId: uuid("bin_id").references(() => binLocationsTable.id),
    binCode: varchar("bin_code"),
    zoneCode: varchar("zone_code"),
    aisleCode: varchar("aisle_code"),
    shelfCode: varchar("shelf_code"),
    rackCode: varchar("rack_code"),
    levelCode: varchar("level_code"),
    positionCode: varchar("position_code"),
    sequence: integer("sequence"),
});
export const pickListLocationsTable = pgTable("pick_list_locations", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    binId: uuid("bin_id").references(() => binLocationsTable.id),
    binCode: varchar("bin_code").notNull(),
    zoneCode: varchar("zone_code"),
    sequence: integer("sequence"),
    status: varchar("status").notNull(),
});
export const pickTasksTable = pgTable("pick_tasks", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    taskId: varchar("task_id").notNull(),
    assignedPickerId: uuid("assigned_picker_id"),
    status: varchar("status").notNull(),
    sequence: integer("sequence"),
    totalItems: integer("total_items"),
    pickedItems: integer("picked_items"),
    createdAt: timestamp("created_at").defaultNow(),
    startedAt: timestamp("started_at"),
    completedAt: timestamp("completed_at"),
});
export const pickRoutesTable = pgTable("pick_routes", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    totalDistance: numeric("total_distance"),
    estDuration: integer("est_duration"),
    optimizationAlgorithm: varchar("optimization_algorithm"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const pickRouteStepsTable = pgTable("pick_route_steps", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    routeId: uuid("route_id").references(() => pickRoutesTable.id),
    sequence: integer("sequence"),
    binId: uuid("bin_id").references(() => binLocationsTable.id),
    binCode: varchar("bin_code").notNull(),
    zoneCode: varchar("zone_code"),
    estDistanceToNext: numeric("est_distance_to_next"),
    status: varchar("status").notNull(),
});
export const pickAssignmentsTable = pgTable("pick_assignments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    pickerId: uuid("picker_id").notNull(),
    assignedAt: timestamp("assigned_at").defaultNow(),
    assignedBy: uuid("assigned_by"),
    status: varchar("status").notNull(),
});
export const pickProgressTable = pgTable("pick_progress", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    totalLocations: integer("total_locations"),
    visitedLocations: integer("visited_locations"),
    totalItems: integer("total_items"),
    pickedItems: integer("picked_items"),
    skippedItems: integer("skipped_items"),
    shortPickedItems: integer("short_picked_items"),
    completionRate: numeric("completion_rate"),
    updatedAt: timestamp("updated_at").defaultNow(),
});
export const pickScanHistoryTable = pgTable("pick_scan_history", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    pickerId: uuid("picker_id"),
    scannedCode: varchar("scanned_code").notNull(),
    scanType: varchar("scan_type").notNull(),
    isSuccess: boolean("is_success").notNull(),
    errorMessage: text("error_message"),
    scannedAt: timestamp("scanned_at").defaultNow(),
    metadata: jsonb("metadata"),
});
export const pickShortPicksTable = pgTable("pick_short_picks", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    itemId: uuid("item_id").references(() => pickListItemsTable.id),
    productId: uuid("product_id").references(() => productVariantsTable.id),
    sku: varchar("sku").notNull(),
    binId: uuid("bin_id").references(() => binLocationsTable.id),
    binCode: varchar("bin_code"),
    orderedQuantity: numeric("ordered_quantity").notNull(),
    pickedQuantity: numeric("picked_quantity").notNull(),
    shortQuantity: numeric("short_quantity").notNull(),
    reason: varchar("reason").notNull(),
    reportedBy: uuid("reported_by"),
    reportedAt: timestamp("reported_at").defaultNow(),
    status: varchar("status").notNull(),
    approvedBy: uuid("approved_by"),
    approvedAt: timestamp("approved_at"),
});
export const pickActivityLogsTable = pgTable("pick_activity_logs", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickListId: uuid("pick_list_id").references(() => pickListsTable.id),
    action: varchar("action").notNull(),
    description: text("description"),
    userId: uuid("user_id"),
    userName: varchar("user_name"),
    timestamp: timestamp("timestamp").defaultNow(),
    metadata: jsonb("metadata"),
});
export const pickerPerformanceTable = pgTable("picker_performance", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickerId: uuid("picker_id").notNull(),
    pickerName: varchar("picker_name"),
    date: date("date").notNull(),
    wavesCompleted: integer("waves_completed").default(0),
    listsCompleted: integer("lists_completed").default(0),
    itemsPicked: integer("items_picked").default(0),
    unitsPicked: integer("units_picked").default(0),
    pickDurationSeconds: integer("pick_duration_seconds").default(0),
    accuracyRate: numeric("accuracy_rate").default("100"),
});
export const pickerWorkloadsTable = pgTable("picker_workloads", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    pickerId: uuid("picker_id").notNull(),
    pickerName: varchar("picker_name"),
    activeListsCount: integer("active_lists_count").default(0),
    activeTasksCount: integer("active_tasks_count").default(0),
    totalUnitsAssigned: integer("total_units_assigned").default(0),
});
export const packingWorkbenchesTable = pgTable("packing_workbenches", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    waveId: uuid("wave_id").references(() => pickWavesTable.id),
    packListId: uuid("pack_list_id").references(() => pickListsTable.id),
    workbenchName: varchar("workbench_name").notNull(),
    workbenchCode: varchar("workbench_code").notNull().unique(),
    packStation: varchar("pack_station").notNull(),
    priority: varchar("priority").notNull(),
    status: varchar("status").notNull(),
    description: text("description"),
    packingMethod: varchar("packing_method"),
    cartonType: varchar("carton_type"),
    cartonLength: numeric("carton_length"),
    cartonWidth: numeric("carton_width"),
    cartonHeight: numeric("carton_height"),
    cartonCapacity: numeric("carton_capacity"),
    cartonWeightLimit: numeric("carton_weight_limit"),
    autoCartonGeneration: boolean("auto_carton_generation").default(true),
    assignedPackerId: uuid("assigned_packer_id"),
    dueDate: timestamp("due_date"),
    estPackingTime: integer("est_packing_time"),
    printPackingSlipAuto: boolean("print_packing_slip_auto").default(false),
    printShippingLabelsAuto: boolean("print_shipping_labels_auto").default(false),
    requireQualityCheck: boolean("require_quality_check").default(false),
    notifyOnCompletion: boolean("notify_on_completion").default(false),
    sendToShipmentWorkbench: boolean("send_to_shipment_workbench").default(false),
    createdBy: uuid("created_by"),
    createdAt: timestamp("created_at").defaultNow(),
    startedAt: timestamp("started_at"),
    completedAt: timestamp("completed_at"),
    cancelledAt: timestamp("cancelled_at"),
    version: integer("version").default(1),
});
export const packingWorkbenchOrdersTable = pgTable("packing_workbench_orders", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    orderId: uuid("order_id").references(() => salesOrdersTable.id),
    orderNo: varchar("order_no").notNull(),
    customerName: varchar("customer_name"),
    orderDate: timestamp("order_date"),
    totalItems: integer("total_items"),
    dueDate: timestamp("due_date"),
    priority: varchar("priority"),
    status: varchar("status"),
});
export const packingWorkbenchItemsTable = pgTable("packing_workbench_items", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    orderId: uuid("order_id").references(() => salesOrdersTable.id),
    productId: uuid("product_id").references(() => productVariantsTable.id),
    productName: varchar("product_name"),
    sku: varchar("sku").notNull(),
    imageUrl: varchar("image_url"),
    quantityOrdered: numeric("quantity_ordered").notNull(),
    quantityPicked: numeric("quantity_picked").default("0"),
    quantityPacked: numeric("quantity_packed").default("0"),
    quantityRemaining: numeric("quantity_remaining").notNull(),
    status: varchar("status").notNull(),
});
export const packingCartonsTable = pgTable("packing_cartons", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    cartonCode: varchar("carton_code").notNull(),
    cartonType: varchar("carton_type").notNull(),
    length: numeric("length"),
    width: numeric("width"),
    height: numeric("height"),
    weight: numeric("weight"),
    status: varchar("status").notNull(),
    utilizationRate: numeric("utilization_rate"),
    sealedAt: timestamp("sealed_at"),
    sealedBy: uuid("sealed_by"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const packingCartonItemsTable = pgTable("packing_carton_items", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    cartonId: uuid("carton_id").references(() => packingCartonsTable.id),
    orderId: uuid("order_id").references(() => salesOrdersTable.id),
    productId: uuid("product_id").references(() => productVariantsTable.id),
    sku: varchar("sku").notNull(),
    quantityPacked: numeric("quantity_packed").notNull(),
    packedAt: timestamp("packed_at").defaultNow(),
    packedBy: uuid("packed_by"),
});
export const packingAssignmentsTable = pgTable("packing_assignments", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    packerId: uuid("packer_id").notNull(),
    assignedAt: timestamp("assigned_at").defaultNow(),
    assignedBy: uuid("assigned_by"),
    status: varchar("status").notNull(),
});
export const packingProgressTable = pgTable("packing_progress", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    totalOrders: integer("total_orders"),
    totalItems: integer("total_items"),
    packedItems: integer("packed_items"),
    remainingItems: integer("remaining_items"),
    shortPickedItems: integer("short_picked_items"),
    overpackedItems: integer("overpacked_items"),
    completionRate: numeric("completion_rate"),
    updatedAt: timestamp("updated_at").defaultNow(),
});
export const packingScanHistoryTable = pgTable("packing_scan_history", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    packerId: uuid("packer_id"),
    scannedCode: varchar("scanned_code").notNull(),
    scanType: varchar("scan_type").notNull(),
    isSuccess: boolean("is_success").notNull(),
    errorMessage: text("error_message"),
    scannedAt: timestamp("scanned_at").defaultNow(),
    metadata: jsonb("metadata"),
});
export const packingShortPicksTable = pgTable("packing_short_picks", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    itemId: uuid("item_id").references(() => packingWorkbenchItemsTable.id),
    productId: uuid("product_id").references(() => productVariantsTable.id),
    sku: varchar("sku").notNull(),
    orderedQuantity: numeric("ordered_quantity").notNull(),
    packedQuantity: numeric("packed_quantity").notNull(),
    shortQuantity: numeric("short_quantity").notNull(),
    reason: varchar("reason").notNull(),
    reportedBy: uuid("reported_by"),
    reportedAt: timestamp("reported_at").defaultNow(),
    status: varchar("status").notNull(),
    approvedBy: uuid("approved_by"),
    approvedAt: timestamp("approved_at"),
});
export const packingOverpacksTable = pgTable("packing_overpacks", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    itemId: uuid("item_id").references(() => packingWorkbenchItemsTable.id),
    productId: uuid("product_id").references(() => productVariantsTable.id),
    sku: varchar("sku").notNull(),
    orderedQuantity: numeric("ordered_quantity").notNull(),
    packedQuantity: numeric("packed_quantity").notNull(),
    overQuantity: numeric("over_quantity").notNull(),
    reason: varchar("reason").notNull(),
    reportedBy: uuid("reported_by"),
    reportedAt: timestamp("reported_at").defaultNow(),
});
export const packingDocumentsTable = pgTable("packing_documents", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    documentType: varchar("document_type").notNull(),
    documentName: varchar("document_name").notNull(),
    documentUrl: varchar("document_url").notNull(),
    downloadUrl: varchar("download_url"),
    generatedAt: timestamp("generated_at").defaultNow(),
    generatedBy: uuid("generated_by"),
    version: integer("version").default(1),
});
export const packingActivityLogsTable = pgTable("packing_activity_logs", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    action: varchar("action").notNull(),
    description: text("description"),
    userId: uuid("user_id"),
    userName: varchar("user_name"),
    timestamp: timestamp("timestamp").defaultNow(),
});
export const packerPerformanceTable = pgTable("packer_performance", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    packerId: uuid("packer_id").notNull(),
    packerName: varchar("packer_name").notNull(),
    workbenchesCompleted: integer("workbenches_completed").default(0),
    itemsPacked: integer("items_packed").default(0),
    totalSeconds: integer("total_seconds").default(0),
    accuracyRate: numeric("accuracy_rate").default("100"),
    avgItemsPerHour: numeric("avg_items_per_hour").default("0"),
});
export const packerWorkloadsTable = pgTable("packer_workloads", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    packerId: uuid("packer_id").notNull(),
    packerName: varchar("packer_name").notNull(),
    activeWorkbenches: integer("active_workbenches").default(0),
    pendingWorkbenches: integer("pending_workbenches").default(0),
    totalAssignedItems: integer("total_assigned_items").default(0),
});
export const packingNotesTable = pgTable("packing_notes", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    workbenchId: uuid("workbench_id").references(() => packingWorkbenchesTable.id),
    noteType: varchar("note_type").notNull(),
    noteText: text("note_text").notNull(),
    authorId: uuid("author_id"),
    authorName: varchar("author_name"),
    createdAt: timestamp("created_at").defaultNow(),
});
export const packStationUtilizationTable = pgTable("pack_station_utilization", {
    id: uuid("id").primaryKey(),
    companyId: uuid("company_id").notNull(),
    warehouseId: uuid("warehouse_id").references(() => warehousesTable.id),
    stationCode: varchar("station_code").notNull(),
    utilizationRate: numeric("utilization_rate").default("0"),
    activeWorkbenches: integer("active_workbenches").default(0),
    completedWorkbenches: integer("completed_workbenches").default(0),
    updatedAt: timestamp("updated_at").defaultNow(),
});
